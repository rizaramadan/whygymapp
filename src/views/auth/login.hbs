<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - WhyGym</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="min-h-screen flex flex-col bg-gradient-to-br from-green-50 to-white">
    <!-- Sidebars -->
    <div class="hidden lg:block fixed left-0 top-0 h-full w-1/6 bg-green-50/30"></div>
    <div class="hidden lg:block fixed right-0 top-0 h-full w-1/6 bg-green-50/30"></div>

    <!-- Main content area -->
    <div class="flex-1 flex flex-col px-4 py-4 sm:py-8">
        <!-- Logo -->
        <div class="w-full max-w-2xl mx-auto">
            <h1 class="text-3xl font-bold text-green-800 text-center mb-8">WhyGym</h1>

            <!-- Main form container -->
            <div class="bg-white rounded-2xl shadow-lg p-8 space-y-6">
                <div id="loginForm" class="space-y-6">
                    <h2 class="text-2xl font-semibold text-green-800 text-center">Welcome</h2>
                    <p class="text-sm text-green-600/80 text-center">
                        Enter your email to receive otp on your email
                    </p>
                    
                    <form hx-post="/auth/otp/create"
                          hx-target="#loginForm"
                          hx-swap="outerHTML"
                          hx-indicator="#spinner"
                          class="space-y-6">    
                        
                        <div class="space-y-2">
                            <label for="email" class="block text-sm font-medium text-green-700">
                                Email address
                            </label>
                            <input type="email" 
                                   name="email"
                                   class="w-full rounded-xl border-green-200 shadow-sm focus:border-green-500 focus:ring-green-500 bg-green-50/30 px-4 py-3" 
                                   required
                                   placeholder="your@email.com">
                        </div>
                        
                        <button class="w-full py-3 px-4 border border-transparent rounded-xl text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition duration-150 ease-in-out"
                                type="submit" >
                            <span class="htmx-indicator-hide">Send Login Code</span>
                            <img  id="spinner" class="htmx-indicator" src="https://htmx.org/img/bars.svg"/>
                        </button>
                    </form>

                    <p class="text-sm text-green-600/60 text-center">
                        By continuing, you agree to our Terms of Service
                    </p>
                </div>
            </div>

            <!-- Footer -->
            <footer class="mt-8">
                <p class="text-sm text-green-600/60 text-center">
                    © 2024 WhyGym. All rights reserved.
                </p>
            </footer>
        </div>
    </div>
</body>
</html> 