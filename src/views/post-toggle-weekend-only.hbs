<div class="toggle-card border-2 border-gray-200">
    <div class="flex items-center justify-between mb-4">
        <div>
            <h3 class="text-lg font-medium">Weekend Only Access</h3>
        </div>
        <button 
            class="toggle-button px-4 py-2 rounded-full flex items-center gap-2 font-medium {{#if weekendOnly}}enabled{{else}}disabled{{/if}}"
            hx-post="/weekend-only-toggle"
            hx-target="#toggle-section"
        >
        {{#if weekendOnly}}enabled{{else}}disabled{{/if}}
        </button>
    </div>
    
</div>